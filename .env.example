# Chalishiya High School Management System Environment Variables

# This file should be copied to .env and filled with the actual credentials.
# Do not commit the .env file to the repository.

# Database credentials
DATABASE_URL="postgresql://user:password@host:port/database"

# NextAuth.js secret
# Generate a new secret for production: `openssl rand -base64 32`
NEXTAUTH_SECRET="your-nextauth-secret"
NEXTAUTH_URL="http://localhost:3000"

# API endpoints (if any)
# EXAMPLE_API_ENDPOINT="https://api.example.com"